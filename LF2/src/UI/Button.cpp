#include "Button.h"

UI_Button::UI_Button(const std::string& imageFile, const sf::IntRect& imageLocationData, const std::string& textValue, int textSize)
{
}

void UI_Button::Animate(sf::RenderWindow& window)
{
}
